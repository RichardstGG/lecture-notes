#!/usr/bin/env python3
"""課堂筆記系統 CLI 入口（https://github.com/RichardstGG/lecture-notes）。
可放進 PATH：ln -s ~/lecture-notes/lec ~/.local/bin/lec"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

if sys.version_info < (3, 11):
    sys.exit("lec 需要 Python 3.11 以上（tomllib）")

from core.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
